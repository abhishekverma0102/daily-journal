<%- include('partials/header') %>
    <h1>Home</h1>
    <p>
        <h4> <%= StartingContent %> <br><br>
        > To compose you just need to click on below button.
    </p>
    <h5> <a class="composelink" href="/compose">Compose</a></h5>

        <% thePost.forEach(function (post) { %>
        <h1> <%=  post.title %> </h1>
        <p>
            <%= post.content.substring(0,100)+". . ." %>
             <a href="/posts/<%= post._id %>">Read More</a>
        </p>
        <% }) %>
    
    <%- include('partials/footer') %>